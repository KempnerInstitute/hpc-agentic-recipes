# Point Claude Code at GLM-4.6 on H200. Usage:
#   export NODE=<the node serving it>
#   source recipes/GLM-4.6-FP8/h200-4/client.env
#   claude
_S="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$_S/../../../common/defaults.sh"
KEY_NAME=GLM-4.6-FP8-h200-4
source "$_S/../../../common/lib/api_key.sh"
_NODE="${NODE:-${GLM46_NODE:-}}"
[ -n "$_NODE" ] || echo "set NODE to the node serving glm-4.6 before sourcing this file" >&2

export ANTHROPIC_BASE_URL="http://${_NODE}:${API_PORT}"
export ANTHROPIC_AUTH_TOKEN="$VLLM_API_KEY"
export ANTHROPIC_MODEL=glm-4.6
export ANTHROPIC_SMALL_FAST_MODEL=glm-4.6
export CLAUDE_CODE_MAX_CONTEXT_TOKENS=202752

export CLAUDE_CODE_ATTRIBUTION_HEADER=0

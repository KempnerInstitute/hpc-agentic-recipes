# Point Claude Code at Qwen3-235B-A22B on an RTX PRO 6000 node. Usage:
#   export NODE=<the node serving it>
#   source recipes/Qwen3-235B-A22B/rtx-8/client.env
#   claude
_S="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$_S/../../../common/defaults.sh"
KEY_NAME=Qwen3-235B-A22B-rtx-8
source "$_S/../../../common/lib/api_key.sh"
_NODE="${NODE:-${QWEN3_235B_NODE:-}}"
[ -n "$_NODE" ] || echo "set NODE to the node serving qwen3-235b before sourcing this file" >&2

export ANTHROPIC_BASE_URL="http://${_NODE}:${API_PORT}"
export ANTHROPIC_AUTH_TOKEN="$VLLM_API_KEY"
export ANTHROPIC_MODEL=qwen3-235b
export ANTHROPIC_SMALL_FAST_MODEL=qwen3-235b

export CLAUDE_CODE_ATTRIBUTION_HEADER=0

# The client assumes and enforces a 200k window for an unrecognized served name, five times what this
# endpoint serves, so without this it overflows the context instead of compacting.
export CLAUDE_CODE_MAX_CONTEXT_TOKENS=40960
export CLAUDE_CODE_MAX_OUTPUT_TOKENS=4096

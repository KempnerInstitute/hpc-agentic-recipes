# Source this, then run `claude`, to point Claude Code at Gemma-4-26B-A4B-it (single GPU).
# Node and port come from scripts/config.sh; the API key comes from secrets/vllm_api_key.
_REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
source "$_REPO/scripts/config.sh" 2>/dev/null || true
export ANTHROPIC_BASE_URL="http://${GEMMA4_NODE}:${API_PORT}"
export ANTHROPIC_AUTH_TOKEN="$(cat "$_REPO/secrets/vllm_api_key" 2>/dev/null)"
export ANTHROPIC_MODEL=gemma-4-26b
export ANTHROPIC_SMALL_FAST_MODEL=gemma-4-26b
